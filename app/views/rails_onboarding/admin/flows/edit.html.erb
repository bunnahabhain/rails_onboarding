<% content_for :page_title, "Edit Flow: #{@flow[:name]}" %>

<% content_for :header_actions do %>
  <%= link_to "← Back to Flows", admin_flows_path, class: "admin-btn admin-btn-secondary" %>
<% end %>

<%= render 'form' %>
